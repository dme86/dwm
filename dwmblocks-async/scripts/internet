STATUS=""
IF=$(ip link show | grep "state UP" | awk '{print $2}' | sed 's/://g')
IF_NUM=$(echo "$IF" | wc -l)


if [ $IF_NUM = "0" ];
then
  STATUS="no internet[!]"
else
  STATUS=$(echo "$IF" | paste -s -d, -)
fi

echo "$STATUS"
